name: CI/CD Deployment with ServiceNow Governance

on:
  push:
    branches: [ "main" ]

jobs:
  build_and_test:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout Code
        uses: actions/checkout@v4

      - name: Run Unit Tests
        run: echo "Executing test suites..."

  create_change_request:
    runs-on: ubuntu-latest
    needs: build_and_test
    steps:
      - name: ServiceNow DevOps Change Request
        uses: ServiceNow/servicenow-devops-change@v4.0.0
        with:
          instance-url: ${{ secrets.SN_INSTANCE_URL }}
          tool-id: ${{ secrets.SN_ORCHESTRATION_TOOL_ID }}
          devops-integration-token: ${{ secrets.SN_DEVOPS_INTEGRATION_TOKEN }}
          context-github: ${{ toJson(github) }}
          job-name: 'create_change_request'
          change-request-details: '{ "short_description": "Automated pipeline deployment", "description": "Deploying main branch updates.", "priority": "3" }'

  deploy_to_production:
    runs-on: ubuntu-latest
    needs: create_change_request
    environment: prod
    steps:
      - name: Execute Deployment
        run: echo "Deploying artifacts to production environment..."
